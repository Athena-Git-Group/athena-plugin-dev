# CONTEXT.md — Flow 主鏈 Ubiquitous Language

本檔是對使用者回報時的術語單一來源。每個定義只寫兩件事：**這對使用者代表什麼**、
**需不需要使用者做什麼**。內部機制細節在 `skills/athena-flow/references/`，
由 AI 自行消化——不得原樣轉述給使用者；使用者問起才展開，且仍用本表的語言講。

## 流程與路由

| 術語 | 對使用者的意義 |
|------|---------------|
| flow | 一鍵自動流程：使用者下一次指令，系統自己跑完評分到出貨 |
| orchestrator（流程總控） | 統籌的主 AI：派工、看結果、做決策，在每個關卡向使用者回報 |
| stage | 流程的一站：評分 → 規格 → 計畫 → 施工 → 驗證 → 審查 → 出貨 |
| point | 第一站評分：判斷這個需求要走多完整的流程 |
| verdict | 評分結論，四種：極小改動／直接做／做完要驗／先寫規格 |
| Weight Class（路由） | verdict 對應的三條路線：Minimal（最短）、Lightweight（標準）、Full（完整） |
| gate | 每站出口的關卡：過了才往下走；沒過會自動補救或停下來問使用者 |
| slug | 這次任務的短代號，所有產出檔名都掛它 |

## 交接物

| 術語 | 對使用者的意義 |
|------|---------------|
| handoff | 每站完成時寫的交接報告，下一站只看這份；出貨後系統會自動清掉 |
| Gate Verdict | 交接報告裡的結論欄：PASS 或 FAIL |
| mini-handoff | 單一施工段落完成時寫的小交接 |
| flow context | 系統記錄「目前跑到哪一站」的狀態檔 |
| point report | 評分報告；沒有它，系統會擋住任何改程式的動作 |

## 施工（Full 路線才有分段）

| 術語 | 對使用者的意義 |
|------|---------------|
| phase / phase 卡 | 規格拆成的施工段落，一張卡一段 |
| phase loop | 逐卡施工：每張卡由全新的 AI 做，做完存檔才拿下一張 |
| 依賴圖（DAG） | 卡片的先後順序；沒有先後關係的卡會同時施工 |
| smoke test | 每段施工完的快速檢查，沒過會自動重試 |
| self-review | 極小改動路線由施工 AI 自己過檢查清單 |
| phase retry | 檢查沒過自動重做，最多 2 次；還不行就停下來問使用者 |

## 平行施工

| 術語 | 對使用者的意義 |
|------|---------------|
| 平行集 | 同時施工的一批段落 |
| worktree 隔離 | 平行施工時每個 AI 在自己的隔離空間做，避免互相干擾；系統自動管理 |
| pre-flight | 施工 AI 開工前先確認自己站對位置，不對就停手回報，系統自動重派 |
| fallback | 隔離空間出問題時，系統自動換備援方式繼續做；全部都不行才停下來回報 |
| merge-back | 平行施工完把成果合併回主線；只有合併衝突才需要使用者出面 |

## 驗證與出貨

| 術語 | 對使用者的意義 |
|------|---------------|
| verify | 獨立 AI 驗證施工成果，只回報問題、不動手改 |
| verify-fix（定點回修） | 驗證沒過就針對被點名的問題回修，最多 2 輪；超過交回使用者決定 |
| review | 獨立 AI 做程式審查；發現問題就停下來交給使用者，不自動硬修 |
| review-ship | 標準路線把審查和出貨合併成一站的作法 |
| merge_target | 成果要合併進哪個分支——這是流程中唯一固定要使用者回答的問題 |
| ship | 最後一站：推上遠端並合併；整個流程只有這站會 push |
| pre-build / post-build | 系統自動做的 git 雜務（開分支、存檔），不需要使用者介入 |

## 收尾與追蹤

| 術語 | 對使用者的意義 |
|------|---------------|
| run | 一次完整流程 |
| run trace | 每次流程結束自動留一筆執行紀錄，供事後分析 |
| Failure Taxonomy | 失敗原因的固定分類標籤，寫進執行紀錄 |
| Handoff GC | 收尾時自動清理已完成流程的交接檔 |
