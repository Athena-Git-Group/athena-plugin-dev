# language: zh-TW
功能: {Entity} — {operation}
  作為 {角色}
  我要 {操作描述}
  以便 {目的}

  # {spec_reference}

  背景:
    假設 {前置狀態}

  Rule: 前置 - {precondition 驗證規則}
    場景大綱: {失敗行為摘要}
      當 {operation}:
        | col_a       | col_b   |
        | <col_a_val> | <col_b> |
      那麼 操作失敗，violation_type 為 "<violation>"

      例子:
        | col_a_val | col_b | violation        |
        | ...       | ...   | INVALID_ARGUMENT |

  Rule: 後置 - {postcondition 行為規則}
    # 單行用 場景，多行 data-driven 用 場景大綱
    場景: {單行成功行為}
      當 ...
      那麼 ...

    場景大綱: {多行成功行為摘要}
      當 {operation}:
        | col_a       | col_b   |
        | <col_a_val> | <col_b> |
      那麼 操作成功，結果符合:
        | col_x        | col_y        |
        | <expected_x> | <expected_y> |

      例子:
        | col_a_val | col_b | expected_x | expected_y |
        | ...       | ...   | ...        | ...        |

  # 變數參照範例：
  #   | actor_id   | name |
  #   | $使用者.id | A    |    ← 引用 背景 中建立的「使用者」actor 的 id
