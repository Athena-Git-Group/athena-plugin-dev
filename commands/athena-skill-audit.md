---
description: Skill 品質健檢（L1+L2 靜態檢查）— 對 .athena/skills/ 下的 skill 做 frontmatter / 命名 / stage 契約 audit
argument-hint: [skill-name]  # 省略則全掃
---

Invoke the `athena-skill-audit` skill. Run a static audit (L1 structure + L2 stage-contract adherence) over the skills in `.athena/skills/`. Use the three-tier advisory format (✅ / 🟡 / 💡); do not produce a PASS/FAIL verdict and do not block any flow.

Target:

$ARGUMENTS
