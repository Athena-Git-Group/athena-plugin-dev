# language: zh-TW
# 示範檔：展示本版三理念 —— Spec by Example（具體值）、邊界優先（QA shift-left）、善用場景大綱。
# 領域沿用 api stage 的訂單範例（apply_refund = POST /orders/{id}/apply-refund）。
# 判定值來源（覆蓋矩陣應回指）：
#   - 退款金額 0 < amount <= 訂單金額        ← spec.md#FR-001 / openapi.yaml#Refund.amount
#   - 可退款狀態：僅「已付款」                ← spec.md#資料維度-訂單狀態轉移
#   - 訂單狀態 enum：待付款/已付款/已出貨/已取消/已退款 ← spec.md#資料維度-訂單狀態-enum
#   - 一張訂單僅能退款一次                    ← spec.md#資料維度-退款唯一性

@backend
功能: 訂單退款
  身為 客服人員
  我想要 對已付款的訂單發起退款
  以便 在客戶要求時退回款項，且不會退超過訂單金額

  背景:
    假設 系統中存在訂單 ORD-001，金額為 1000 元、狀態為「已付款」

  # ── 規則一：退款金額必須在 (0, 訂單金額] 之間 ───────────────────
  Rule: 退款金額需大於 0 且不得超過訂單金額

    # 邊界 / 錯誤列排前（@error、@boundary 在 @happy 之前）—— QA shift-left
    場景大綱: 依退款金額判定退款結果
      當 我對訂單 ORD-001 申請退款 <申請金額> 元
      那麼 退款結果應為 <結果>
      而且 回應錯誤碼應為 <錯誤碼>

      @error
      例子: 非正數金額（語法 / 型別違規 → 400）
        | 申請金額 | 結果 | 錯誤碼            |
        | 0        | 拒絕 | INVALID_AMOUNT    |
        | -1       | 拒絕 | INVALID_AMOUNT    |

      @boundary
      例子: 金額邊界（min 與 max±1）
        | 申請金額 | 結果 | 錯誤碼                |
        | 1        | 成功 | -                     |
        | 1000     | 成功 | -                     |
        | 1001     | 拒絕 | REFUND_EXCEEDS_TOTAL  |

      @happy
      例子: 代表性正常案例
        | 申請金額 | 結果 | 錯誤碼 |
        | 500      | 成功 | -      |

    @boundary
    場景: 全額退款後訂單轉為「已退款」
      當 我對訂單 ORD-001 申請退款 1000 元
      那麼 退款應成功（HTTP 200）
      而且 訂單 ORD-001 的狀態應變為「已退款」

  # ── 規則二：只有「已付款」訂單可退款（狀態機）────────────────────
  Rule: 僅「已付款」狀態的訂單可被退款

    @error
    場景: 待付款訂單不可退款（非法狀態轉移 → 409）
      假設 系統中存在訂單 ORD-002，金額為 800 元、狀態為「待付款」
      當 我對訂單 ORD-002 申請退款 800 元
      那麼 退款應被拒絕
      而且 回應錯誤碼應為 INVALID_STATE_TRANSITION（HTTP 409）
      而且 訂單 ORD-002 的狀態仍為「待付款」

    @error
    場景: 已取消訂單不可退款（非法狀態轉移 → 409）
      假設 系統中存在訂單 ORD-003，金額為 600 元、狀態為「已取消」
      當 我對訂單 ORD-003 申請退款 600 元
      那麼 退款應被拒絕
      而且 回應錯誤碼應為 INVALID_STATE_TRANSITION（HTTP 409）

    # 缺則回報示範：spec.md 的狀態轉移表沒涵蓋「已出貨可否退款」。
    # 不自行假設 —— 標 @待釐清、暫不可執行，並寫進 handoffs/gherkin.md 的「回饋訊號」段。
    @error @待釐清
    場景: 已出貨訂單可否退款（需求未定義）
      假設 系統中存在訂單 ORD-004，金額為 1200 元、狀態為「已出貨」
      當 我對訂單 ORD-004 申請退款 1200 元
      那麼 退款結果應為 待釐清
      # 待釐清:「已出貨」訂單能否退款？若可，是否需先處理退貨？（spec.md#資料維度-訂單狀態轉移 未涵蓋）

  # ── 規則三：一張訂單只能退款一次（唯一性 → 409）──────────────────
  Rule: 同一張訂單僅能成功退款一次

    @error
    場景: 重複退款被拒
      假設 訂單 ORD-001 已成功退款 1000 元、狀態為「已退款」
      當 我再次對訂單 ORD-001 申請退款 1000 元
      那麼 退款應被拒絕
      而且 回應錯誤碼應為 ALREADY_REFUNDED（HTTP 409）

  # ── 規則四：對不存在的訂單退款 → 404 ───────────────────────────
  Rule: 對不存在的訂單退款回報找不到

    @error
    場景: 退款目標不存在
      當 我對訂單 ORD-999 申請退款 100 元
      那麼 退款應被拒絕
      而且 回應錯誤碼應為 NOT_FOUND（HTTP 404）
